{% extends '../layout.twig' %}

{% block body %}
    <h1>{{title}}</h1>

    name: {{ doctor.name }}
    <br>
    phone: {{ doctor.phone }}

    <ul>
        {% for visit in visits %}
            <li><a href="/visits/{{ visit.visit_id }}">{{ visit.date }}</a> | <a href="/patients/{{ visit.patient_id }}">{{ visit.patient }}</a></li>
        {% endfor %}
    </ul>


    {#<datetime-picker native-on-mobile></datetime-picker>#}
    {#<add-button color="blue"></add-button>#}
{% endblock %}
