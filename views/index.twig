{% extends 'layout.twig' %}

{% block body %}
  <h1>{{title}}</h1>

  <ul>
    {% for treatment in treatments %}
      <li>{{ treatment.patient|e }} | {{ treatment.doctor }} | {{ treatment.date }}</li>
    {% endfor %}
  </ul>


  {#<datetime-picker native-on-mobile></datetime-picker>#}
  <add-button color="blue"></add-button>
{% endblock %}
